@model IEnumerable<AlimentandoEsperanzas.Models.Donationtype>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DonationType1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DonationType1)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.DonationTypeId">Edit</a> |
                    <a asp-action="Details" class="btn btn-light" asp-route-id="@item.DonationTypeId">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.DonationTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
